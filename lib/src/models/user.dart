class User {
  final String name, email, imageUrl, phoneNumber, gender;
  final bool isOnline;

  User({
    this.name,
    this.email,
    this.imageUrl,
    this.phoneNumber,
    this.gender,
    this.isOnline,
  });
}
